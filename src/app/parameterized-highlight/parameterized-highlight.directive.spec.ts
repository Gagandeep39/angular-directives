import { ParameterizedHighlightDirective } from './parameterized-highlight.directive';

describe('ParameterizedHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ParameterizedHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
